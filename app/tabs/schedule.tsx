import { useEffect } from "react";
import { router } from "expo-router";

export default function ScheduleTab() {
    // useEffect(() => {
    //   router.replace('/screens/timetable');
    // }, []);
    return null;
}
